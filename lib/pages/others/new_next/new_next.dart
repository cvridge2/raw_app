import 'package:flutter/material.dart';

class new_next_page extends StatelessWidget {
  const new_next_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.red,
    );
  }
}

